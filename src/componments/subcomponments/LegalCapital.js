import React from "react";

import { Alert } from "react-bootstrap";

export default LegalCapital => (
    <Alert>
        Capital at <Alert.Link href="#">Risk</Alert.Link>. Not protected by the FSCS. Past performance is not an indicator of future results.
    </Alert>
)